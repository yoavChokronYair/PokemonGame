<Window
    x:Class="PokemonGameModel.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Map="clr-namespace:PokemonGameModel.ViewModel.Map"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PokemonGameModel"
    xmlns:local1="clr-namespace:PokemonGameModel.Views"
    xmlns:local2="clr-namespace:PokemonGameModel.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:PokemonGameModel.ViewModel"
    xmlns:vm1="clr-namespace:PokemonGameModel.ViewModel.BattleMenu"
    Title="MainWindow"
    Width="900"
    Height="600"
    d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
    KeyUp="Window_KeyUp"
    Loaded="Window_Loaded"
    mc:Ignorable="d">
    <Grid ShowGridLines="False">

        <Grid.Resources>
            <DataTemplate DataType="{x:Type vm:WildPokemonBattleViewModel}">
                <local2:WildPokemonBattleView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type Map:MapViewModel}">
                <local2:MapViewPage />
            </DataTemplate>
        </Grid.Resources>
        <Frame
            Grid.Row="5"
            Grid.ColumnSpan="2"
            Content="{Binding CurrentViewModel}"
            NavigationUIVisibility="Hidden" />
    </Grid>
</Window>