<Page
    x:Class="PokemonGame.Views.Pages.Trainer.TrainerCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:PokemonGame.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="TrainerCard"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Page.Resources>

        <converters:BadgeIdToImageConverter x:Key="BadgeIdToImageConverter" />

        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />

    </Page.Resources>

    <Grid>

        <Viewbox Stretch="Uniform">

            <Grid Width="600" Height="400">

                <Grid.RowDefinitions>
                    <RowDefinition Height="70" />
                    <RowDefinition Height="56" />
                    <RowDefinition Height="67" />
                    <RowDefinition Height="85" />
                    <RowDefinition Height="52*" />
                    <RowDefinition Height="41*" />
                    <RowDefinition Height="29*" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70" />
                    <ColumnDefinition Width="65" />
                    <ColumnDefinition Width="326" />
                    <ColumnDefinition Width="95*" />
                    <ColumnDefinition Width="47*" />
                </Grid.ColumnDefinitions>

                <!--  Trainer Card Background  -->
                <Image
                    Grid.RowSpan="7"
                    Grid.ColumnSpan="5"
                    RenderOptions.BitmapScalingMode="NearestNeighbor"
                    Source="{Binding TrainerCardImage}" />

                <!--  Trainer ID  -->
                <TextBlock
                    Grid.Column="3"
                    Height="20"
                    Margin="0,50,0,0"
                    VerticalAlignment="Top"
                    Text="{Binding TrainerId}" />

                <!--  Trainer Name  -->
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="2"
                    Height="39"
                    VerticalAlignment="Bottom"
                    FontSize="24"
                    Text="{Binding TrainerName}" />

                <!--  Play Time  -->
                <TextBlock
                    Grid.Row="2"
                    Grid.Column="2"
                    VerticalAlignment="Bottom"
                    FontSize="24"
                    Text="{Binding Money}" />
                <TextBlock
                    Grid.Row="3"
                    Grid.Column="2"
                    VerticalAlignment="Bottom"
                    FontSize="24"
                    Text="{Binding PlayTime}" />

                <!--  Pokemon Team  -->
                <ItemsControl
                    Grid.Row="4"
                    Grid.Column="1"
                    Grid.ColumnSpan="3"
                    ItemsSource="{Binding Badges}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>

                            <Image
                                Width="40"
                                Height="40"
                                Margin="5"
                                RenderOptions.BitmapScalingMode="NearestNeighbor"
                                Source="{Binding BadgeId, Converter={StaticResource BadgeIdToImageConverter}}"
                                Visibility="{Binding IsUnlocked, Converter={StaticResource BoolToVisibilityConverter}}" />

                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>

            </Grid>

        </Viewbox>

    </Grid>
</Page>