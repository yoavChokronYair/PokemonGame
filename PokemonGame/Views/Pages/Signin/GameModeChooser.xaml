<Page
    x:Class="PokemonGame.Views.Pages.Signin.GameModeChooser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:PokemonGame.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PokemonGame.Views.Pages.Signin"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="GameModeChooser"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="#4a5194"
    mc:Ignorable="d">

    <Grid>
        <Label
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Content="Game mode"
            FontSize="24" />

        <StackPanel
            Width="350"
            Height="350"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <!--  Story Mode Box  -->
            <controls:BoardBackground
                Width="300"
                Height="125"
                Margin="25"
                BackgroundType="{x:Static controls:BackgroundType.White}"
                BorderThickness="0">

                <Button
                    Margin="25"
                    Background="Transparent"
                    BorderThickness="0"
                    Command="{Binding StoryModeCommand}"
                    Content="Story mode"
                    Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </controls:BoardBackground>

            <!--  Online Mode Box  -->
            <controls:BoardBackground
                Width="300"
                Height="125"
                Margin="25"
                BackgroundType="{x:Static controls:BackgroundType.White}">

                <Button
                    Margin="25"
                    Background="Transparent"
                    BorderThickness="0"
                    Command="{Binding OnlineModeCommand}"
                    Content="Online mode"
                    Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </controls:BoardBackground>

        </StackPanel>
    </Grid>
</Page>
