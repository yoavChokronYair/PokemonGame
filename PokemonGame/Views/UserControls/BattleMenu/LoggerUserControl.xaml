<UserControl
    x:Class="PokemonGame.Views.UserControls.BattleMenu.LoggerUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="150"
    d:DesignWidth="800"
    Focusable="False"
    IsTabStop="False"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="QuestionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="80" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="6,0,0,0" />
            <Setter Property="Background" Value="#222222" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="White" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Focusable" Value="False" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <!--  Background image  -->
        <Image Source="/Assets/Images/TextBackground.png" Stretch="Fill" />

        <!--  Layout: message on left, question buttons on right  -->
        <Grid Margin="30,20,30,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Phase label  -->
            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                FontSize="10"
                FontStyle="Italic"
                Foreground="#AAFFFFFF"
                Text="{Binding PhaseLabel}" />

            <!--  Main message text  -->
            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                VerticalAlignment="Center"
                FontSize="16"
                FontWeight="Bold"
                Foreground="White"
                Text="{Binding CurrentMessage}"
                TextWrapping="Wrap" />

            <!--  YES / NO question buttons  -->
            <StackPanel
                Grid.Row="1"
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Visibility="{Binding IsQuestionOpen, Converter={StaticResource BoolToVis}}">

                <Button
                    Command="{Binding QuestionYesCommand}"
                    Content="YES"
                    Style="{StaticResource QuestionButtonStyle}" />

                <Button
                    Command="{Binding QuestionNoCommand}"
                    Content="NO"
                    Style="{StaticResource QuestionButtonStyle}" />

            </StackPanel>
        </Grid>
    </Grid>
</UserControl>