<UserControl
    x:Class="PokemonGame.Views.UserControls.BattleMenu.LoggerUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PokemonGame.Views.UserControls.BattleMenu"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="150"
    d:DesignWidth="800"
    Focusable="False"
    IsTabStop="False"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid>
        <!--  Background image  -->
        <Image Source="/Assets/Images/TextBackground.png" Stretch="Fill" />

        <!--  Layout: message on left, Next button on right  -->
        <Grid Margin="30,20,30,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Phase label (e.g. "Turn 3 · Action")  -->
            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                FontSize="10"
                FontStyle="Italic"
                Foreground="#AAFFFFFF"
                Text="{Binding PhaseLabel}" />

            <!--  Main message text  -->
            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                VerticalAlignment="Center"
                FontSize="16"
                FontWeight="Bold"
                Foreground="White"
                Text="{Binding CurrentMessage}"
                TextWrapping="Wrap" />

        </Grid>
    </Grid>
</UserControl>
